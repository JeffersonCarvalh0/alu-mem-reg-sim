# ifndef DEFS_H
# define DEFS_H

# define bit bool
# define WORD 32

enum ResultOptions {
    // 1-bit ALU's mux to select its output
    AND, OR, SUM, LESS
};

# endif /* end of include guard: DEFS_H */
